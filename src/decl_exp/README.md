This folder contains the assembly file generated by LFI/Decl toolchain. 

After untar the LFI pre-built binaries, the command used: 

```
lfi-run -a amd64 --output [filename].s --poc [filename].c
```

It's supposed to produce a Decl/LFI-compatible assembly files. But I'll have to double check...
